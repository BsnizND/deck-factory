# Snizco Agency Fixture Style

This folder contains generated fixture assets for the placeholder `snizco-agency` style:

- `template.pptx`: prepared template deck with representative dummy slides.
- `library.pptx`: reusable slide library with `about-us`, `methodology`, and parameterized `case-study` slides.
- `deck-spec.json`: minimal sample deck spec.

These are fixtures for implementation and tests, not final brand assets.
