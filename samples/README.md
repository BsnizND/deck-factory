# Samples

Sample `.pptx` files in this directory are generated fixtures for Deck Factory development.

They are not client templates and do not contain live research. Regenerate them with:

```bash
npm run generate:samples
```
